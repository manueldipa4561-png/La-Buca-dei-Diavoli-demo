(()=>{"use strict";const d=document,root=d.documentElement,header=d.querySelector("[data-header]"),toggle=d.querySelector("[data-toggle]"),nav=d.querySelector("[data-nav]"),progress=d.querySelector("[data-progress]"),chamber=d.querySelector("[data-chamber]");const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches,fine=matchMedia("(pointer:fine)").matches;const close=()=>{if(!toggle||!nav)return;toggle.setAttribute("aria-expanded","false");toggle.classList.remove("is-open");nav.classList.remove("is-open")};if(toggle&&nav){toggle.addEventListener("click",()=>{const open=toggle.getAttribute("aria-expanded")==="true";toggle.setAttribute("aria-expanded",String(!open));toggle.classList.toggle("is-open",!open);nav.classList.toggle("is-open",!open)});nav.querySelectorAll("a").forEach(a=>a.addEventListener("click",close));d.addEventListener("keydown",e=>{if(e.key==="Escape")close()})}const reveals=[...d.querySelectorAll(".reveal")];if(!reduced&&"IntersectionObserver"in window){root.classList.add("motion-ready");const io=new IntersectionObserver((entries,obs)=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add("is-visible");obs.unobserve(e.target)}}),{threshold:.08,rootMargin:"0px 0px -4% 0px"});reveals.forEach(el=>io.observe(el));requestAnimationFrame(()=>reveals.forEach(el=>{const r=el.getBoundingClientRect();if(r.top<innerHeight*.94&&r.bottom>0)el.classList.add("is-visible")}))}else reveals.forEach(el=>el.classList.add("is-visible"));setTimeout(()=>reveals.forEach(el=>el.classList.add("is-visible")),1600);let raf=0;const onScroll=()=>{if(raf)return;raf=requestAnimationFrame(()=>{const y=scrollY;header?.classList.toggle("is-scrolled",y>18);if(progress){const max=Math.max(1,d.documentElement.scrollHeight-innerHeight);progress.style.transform=`scaleX(${Math.min(1,y/max)})`}raf=0})};onScroll();addEventListener("scroll",onScroll,{passive:true});if(chamber&&fine&&!reduced){chamber.addEventListener("pointermove",e=>{const r=chamber.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;chamber.style.transform=`perspective(1100px) rotateX(${(-y*3).toFixed(2)}deg) rotateY(${(x*4).toFixed(2)}deg)`});chamber.addEventListener("pointerleave",()=>chamber.style.transform="")}if(fine&&!reduced)d.querySelectorAll(".magnetic").forEach(el=>{el.addEventListener("pointermove",e=>{const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`translate(${(x*5).toFixed(1)}px,${(y*4).toFixed(1)}px)`});el.addEventListener("pointerleave",()=>el.style.transform="")})})();
